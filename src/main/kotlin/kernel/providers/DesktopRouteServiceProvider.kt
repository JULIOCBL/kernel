package kernel.providers

import kernel.foundation.Application

/**
 * Alias de compatibilidad para el provider viejo de rutas desktop.
 */
class DesktopRouteServiceProvider(app: Application) : RouteServiceProvider(app)
