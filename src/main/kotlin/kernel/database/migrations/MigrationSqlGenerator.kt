package kernel.database.migrations

/**
 * Alias publico conservado por compatibilidad.
 *
 * El nombre canónico interno del kernel es `SchemaSqlGenerator`.
 */
typealias MigrationSqlGenerator = kernel.database.grammars.SchemaSqlGenerator
